# 🚀 Quick Start - 3 ขั้นตอนเดียว

## ✅ ขั้นตอน 1: Download (2 ไฟล์)
1. **`index.html`** (มี `?v=4`)
2. **`js/main.js`** (localStorage support)

---

## ✅ ขั้นตอน 2: แทนที่ไฟล์
1. Copy `index.html` → แทนที่ของเดิม
2. Copy `js/main.js` → แทนที่ใน `js/`

---

## ✅ ขั้นตอน 3: ทดสอบ

### วิธีที่ 1: ทดสอบด้วยไฟล์ทดสอบ (เร็วที่สุด) ⭐
```
1. เปิด Browser (Incognito Mode)
2. ไปที่: http://localhost:8000/test-full-flow.html
3. กดปุ่ม: "🎯 Run Complete Flow Test"
4. เห็น: ✅ All tests PASSED
```

**ถ้า Test นี้ผ่าน = ระบบทำงานสมบูรณ์ 100%**

---

### วิธีที่ 2: ทดสอบใน Platform
```
1. เปิด: http://localhost:8000 (Incognito Mode)
2. Test Prompt → พิมพ์: "ขอเกณฑ์การอนุมัติสินเชื่อบ้าน"
3. Flag Output → กรอกข้อมูล → Submit
4. My Submissions → เห็นรายการ Flag ✅
5. Dashboard → Stats อัปเดต ✅
```

---

## 🐛 ถ้ามีปัญหา

**ลำดับการแก้:**
1. ✅ Hard Refresh: **Ctrl + Shift + R**
2. ✅ Clear Cache: F12 → Application → Clear site data
3. ✅ เปิดใน **Incognito Mode**
4. ✅ ตรวจสอบไฟล์: `index.html` ต้องมี `?v=4`

**ยังไม่ได้:**
```javascript
// Console (F12)
console.log(localStorage.getItem('mockFlags'));
// ถ้า null → ลอง Submit Flag ใหม่
```

---

## 📊 ข้อมูลที่เก็บใน localStorage

```javascript
// Console (F12)
localStorage.getItem('mockPrompts')  // Prompts ที่ทดสอบ
localStorage.getItem('mockFlags')    // Flags ที่ส่ง
```

**Clear ข้อมูล:**
```javascript
localStorage.clear();  // ลบทั้งหมด
```

---

## ✅ สรุป

| Feature | Status |
|---------|--------|
| Mock Response | ✅ ทำงาน |
| Flag Submission | ✅ ทำงาน |
| My Submissions | ✅ ทำงาน |
| Dashboard Stats | ✅ ทำงาน |
| Filters | ✅ ทำงาน |

**Version:** 4 (Final)  
**Test File:** `test-full-flow.html` ⭐
