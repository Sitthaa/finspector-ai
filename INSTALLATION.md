# 📦 คู่มือการติดตั้งและใช้งาน Local Server

## 🎯 สำหรับ: Red Teaming Challenge 2026: FinSpector AI

---

## 📋 ข้อกำหนดเบื้องต้น

ไม่ต้องติดตั้งอะไรเพิ่มเติม! แค่มี:
- ✅ Computer (Windows, Mac, หรือ Linux)
- ✅ Web Browser (Chrome, Firefox, Edge, Safari)

---

## 🚀 วิธีการใช้งาน (3 ขั้นตอน)

### ขั้นตอนที่ 1: Extract ไฟล์

1. **ดับเบิลคลิก** ที่ไฟล์ `red-teaming-platform.zip`
2. **Extract/แตกไฟล์** ออกมา
3. คุณจะได้โฟลเดอร์ชื่อ `red-teaming-platform`

โครงสร้างไฟล์ที่ถูกต้อง:
```
red-teaming-platform/
├── index.html
├── css/
│   └── style.css
├── js/
│   ├── main.js
│   ├── api.js
│   └── taxonomy.js
└── images/
    └── logos-combined.png
```

---

### ขั้นตอนที่ 2: เปิดใช้งาน Local Server

เลือกวิธีใดวิธีหนึ่งตามระบบปฏิบัติการของคุณ:

#### 🪟 Windows

**วิธีที่ 1: ใช้ Python (แนะนำ)**
1. เปิด Command Prompt (กด Windows + R, พิมพ์ `cmd`, Enter)
2. ไปที่โฟลเดอร์โปรเจค:
   ```cmd
   cd C:\path\to\red-teaming-platform
   ```
3. รัน server:
   ```cmd
   python -m http.server 8000
   ```
   หรือถ้าใช้ Python 2:
   ```cmd
   python -m SimpleHTTPServer 8000
   ```

**วิธีที่ 2: ใช้ PHP**
```cmd
cd C:\path\to\red-teaming-platform
php -S localhost:8000
```

**วิธีที่ 3: ใช้ VS Code (ถ้ามี)**
1. เปิดโฟลเดอร์ใน VS Code
2. ติดตั้ง Extension: "Live Server"
3. คลิกขวาที่ `index.html` → "Open with Live Server"

---

#### 🍎 Mac

**วิธีที่ 1: ใช้ Python (แนะนำ)**
1. เปิด Terminal
2. ไปที่โฟลเดอร์โปรเจค:
   ```bash
   cd ~/path/to/red-teaming-platform
   ```
3. รัน server:
   ```bash
   python3 -m http.server 8000
   ```

**วิธีที่ 2: ใช้ PHP**
```bash
cd ~/path/to/red-teaming-platform
php -S localhost:8000
```

---

#### 🐧 Linux

1. เปิด Terminal
2. ไปที่โฟลเดอร์โปรเจค:
   ```bash
   cd /path/to/red-teaming-platform
   ```
3. รัน server:
   ```bash
   python3 -m http.server 8000
   ```

---

### ขั้นตอนที่ 3: เปิดใน Browser

1. เปิด Browser (แนะนำ Chrome)
2. ไปที่ URL:
   ```
   http://localhost:8000
   ```
3. ✅ **เสร็จสิ้น!** คุณจะเห็น UI สวยงามเต็มรูปแบบ

---

## 🎬 วิธีปิด Server

เมื่อใช้งานเสร็จแล้ว:
- กดปุ่ม `Ctrl + C` ใน Terminal/Command Prompt
- Server จะหยุดทำงาน

---

## ⚠️ แก้ปัญหา (Troubleshooting)

### ปัญหา: หน้าเว็บแสดงเป็นข้อความล้วน (ไม่มี CSS)

**สาเหตุ**: เปิดไฟล์ `index.html` โดยตรง (file://)

**วิธีแก้**: ต้องเปิดผ่าน Local Server (http://localhost:8000) เท่านั้น!

---

### ปัญหา: Python ไม่พบ

**วิธีแก้**:
1. ตรวจสอบว่าติดตั้ง Python หรือยัง:
   ```
   python --version
   ```
2. ถ้ายังไม่มี ดาวน์โหลดจาก: https://www.python.org/downloads/
3. หรือใช้วิธีอื่น (PHP, VS Code Live Server)

---

### ปัญหา: Port 8000 ถูกใช้งานแล้ว

**วิธีแก้**: เปลี่ยนเป็น port อื่น เช่น 8080, 3000, 5000

```bash
python3 -m http.server 8080
```

แล้วเปิดที่: `http://localhost:8080`

---

### ปัญหา: โลโก้ไม่แสดง

**วิธีแก้**:
1. ตรวจสอบว่าไฟล์ `images/logos-combined.png` มีอยู่จริง
2. ตรวจสอบโครงสร้างโฟลเดอร์ให้ถูกต้อง
3. ห้ามเปลี่ยนชื่อไฟล์หรือย้ายตำแหน่ง

---

## 🌐 สำหรับการ Demo ให้ผู้อื่นดู

### Demo บนเครื่องตัวเอง (Same Network)

ถ้าต้องการให้เพื่อนร่วมงานเข้าดูผ่าน WiFi เดียวกัน:

1. หา IP Address ของเครื่องคุณ:
   
   **Windows**:
   ```cmd
   ipconfig
   ```
   หา "IPv4 Address" (เช่น 192.168.1.100)
   
   **Mac/Linux**:
   ```bash
   ifconfig | grep inet
   ```
   หา IP ที่ขึ้นต้นด้วย 192.168 หรือ 10.0

2. รัน server ให้รับ connection จากภายนอก:
   ```bash
   python3 -m http.server 8000 --bind 0.0.0.0
   ```

3. แชร์ URL ให้เพื่อน:
   ```
   http://192.168.1.100:8000
   ```
   (เปลี่ยน IP เป็นของคุณ)

---

## 🔒 ความปลอดภัย

⚠️ **คำเตือน**: Local Server นี้ **ไม่ควรเปิดให้ Internet เข้าถึง**

- ✅ ใช้สำหรับ demo ใน Local Network เท่านั้น
- ✅ ปิด server เมื่อไม่ใช้งาน (Ctrl + C)
- ❌ ห้ามเปิด Port Forwarding บน Router

---

## 📊 ข้อมูลเพิ่มเติม

### ความเร็ว
- Local Server รวดเร็วมาก เพราะไม่ต้องผ่าน Internet
- เหมาะสำหรับ demo และพัฒนา

### ข้อมูล
- ข้อมูลทั้งหมดเป็น Mock Data (ข้อมูลจำลอง)
- ไม่มีการเชื่อมต่อ Database จริง
- เหมาะสำหรับการทดสอบและ demo

### การแก้ไข
หากต้องการแก้ไขโค้ด:
1. แก้ไขไฟล์ที่ต้องการ
2. Refresh Browser (F5)
3. เห็นผลทันที!

---

## 🎯 ขั้นตอนสำหรับ Demo

1. **เปิด Server**: `python3 -m http.server 8000`
2. **เปิด Browser**: ไปที่ `http://localhost:8000`
3. **Demo**: ใช้งานตาม DEMO_GUIDE.md
4. **ปิด Server**: กด `Ctrl + C` เมื่อเสร็จ

---

## 📞 ต้องการความช่วยเหลือ?

หากมีปัญหาในการติดตั้ง:

1. ตรวจสอบ **โครงสร้างไฟล์** ให้ถูกต้อง
2. อ่าน **Troubleshooting** ด้านบน
3. ลอง **เปลี่ยนวิธีรัน Server** (Python → PHP → VS Code)
4. ตรวจสอบ **Browser Console** (F12) เพื่อดู errors

---

## ✅ Checklist ก่อน Demo

- [ ] Extract ไฟล์แล้ว
- [ ] โครงสร้างโฟลเดอร์ถูกต้อง
- [ ] รัน Local Server แล้ว
- [ ] เปิดที่ http://localhost:8000 (ไม่ใช่ file://)
- [ ] เห็น UI สวยงาม โลโก้แสดงถูกต้อง
- [ ] ทดสอบส่ง Prompt และ Flag แล้ว
- [ ] อ่าน DEMO_GUIDE.md แล้ว

---

<div align="center">

**🛡️ Red Teaming Challenge 2026: FinSpector AI**

*Ready for Demo!*

</div>
