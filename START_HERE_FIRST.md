# 🎯 เริ่มต้นที่นี่ - Quick Start Guide

## สิ่งที่คุณต้องทำเพียง 2 ขั้นตอน

### ขั้นที่ 1: ดาวน์โหลดโปรเจคทั้งหมด

ใช้ปุ่ม **Publish** บน Genspark แล้วดาวน์โหลดไฟล์ทั้งหมดมา

**หรือ** ดาวน์โหลดไฟล์เหล่านี้:
```
index.html
css/style.css
js/main.js
js/api.js
js/taxonomy.js
images/logos-combined.png
start-server.bat (สำหรับ Windows)
start-server.sh (สำหรับ Mac/Linux)
```

---

### ขั้นที่ 2: เปิด Server และใช้งาน

#### **สำหรับ Windows:**
1. ดับเบิลคลิกไฟล์ **`start-server.bat`**
2. รอสักครู่ จะเห็น Command Prompt เปิดขึ้นมา
3. เปิดเบราว์เซอร์ไปที่: **http://localhost:8000**

#### **สำหรับ Mac/Linux:**
1. เปิด Terminal ในโฟลเดอร์โปรเจค
2. รัน:
   ```bash
   chmod +x start-server.sh
   ./start-server.sh
   ```
3. เปิดเบราว์เซอร์ไปที่: **http://localhost:8000**

---

## ✅ เสร็จแล้ว!

ตอนนี้คุณสามารถ:
- ✅ ใช้งาน Platform ได้เต็มรูปแบบ
- ✅ Demo ให้เพื่อนร่วมงานดู
- ✅ ทดสอบทุก Features
- ✅ ไม่มีการอ้างอิงถึง Genspark หรือ AI Tools

---

## 🎭 วิธี Demo ให้เพื่อนดู (แชร์บนเครือข่ายเดียวกัน)

1. หา IP Address ของคุณ:
   - **Windows**: เปิด CMD พิมพ์ `ipconfig`
   - **Mac/Linux**: เปิด Terminal พิมพ์ `ifconfig`
   - หา IP ที่มีรูปแบบ `192.168.x.x`

2. รัน Server แบบ public:
   ```bash
   python3 -m http.server 8000 --bind 0.0.0.0
   ```

3. บอกเพื่อนให้เปิด:
   ```
   http://[YOUR_IP]:8000
   ตัวอย่าง: http://192.168.1.100:8000
   ```

---

## 📚 เอกสารเพิ่มเติม

หากต้องการอ่านรายละเอียด:

- **STANDALONE_PACKAGE_COMPLETE.md** - สรุปแพ็กเกจทั้งหมด ⭐ แนะนำอ่าน
- **HOW_TO_USE_STANDALONE.md** - วิธีใช้งานละเอียด
- **DEMO_GUIDE.md** - สคริปต์สำหรับการ Demo
- **README.md** - เอกสารโครงการแบบเต็ม

---

## ⚠️ สิ่งสำคัญ

**ห้าม** เปิดไฟล์ `index.html` โดยตรง (ดับเบิลคลิก)!

ต้องเปิดผ่าน HTTP Server เท่านั้น (`http://localhost:8000`)

เหตุผล:
- ❌ CSS จะไม่โหลด
- ❌ JavaScript จะไม่ทำงาน
- ❌ โลโก้จะไม่แสดง

---

## 🆘 ถ้ามีปัญหา

### ปัญหา: Python ไม่ติดตั้ง
**แก้:** ดาวน์โหลด Python จาก https://www.python.org/downloads/

### ปัญหา: Port 8000 ถูกใช้แล้ว
**แก้:** เปลี่ยน Port:
```bash
python3 -m http.server 8080
```
แล้วเปิด http://localhost:8080

### ปัญหา: หน้าตาพัง
**แก้:** ตรวจสอบว่าเปิดผ่าน `http://localhost:8000` (ไม่ใช่ `file:///`)

---

## 🎉 พร้อมแล้ว!

Platform นี้พร้อมใช้งาน 100%

**ลอง Demo ได้เลย!** 🚀

---

**Project:** Red Teaming Challenge 2026 - FinSpector AI  
**For:** ETDA & NECTEC  
**Status:** Production Ready ✅
