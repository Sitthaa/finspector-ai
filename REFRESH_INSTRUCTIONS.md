# 🔄 วิธีอัปเดตไฟล์ใน Local Host

## ปัญหา
เมื่อแก้ไขไฟล์ `js/main.js` ใน Genspark แต่ Local Server ยังโหลดไฟล์เก่า

## วิธีแก้ไข (เลือก 1 วิธี)

### ✅ วิธีที่ 1: Hard Refresh (เร็วที่สุด)
1. เปิดหน้า `http://localhost:8000`
2. กด **Ctrl + Shift + R** (Windows/Linux)
3. หรือ **Cmd + Shift + R** (Mac)
4. หรือ **F12 → คลิกขวา Refresh icon → Empty Cache and Hard Reload**

---

### ✅ วิธีที่ 2: Clear Browser Cache
1. กด **F12** เพื่อเปิด DevTools
2. ไปที่แท็บ **Application** (หรือ **Storage**)
3. คลิก **Clear site data**
4. Refresh หน้าเว็บ (F5)

---

### ✅ วิธีที่ 3: Download ไฟล์ใหม่จาก Genspark
1. ใน Genspark คลิก **Download/Export Project**
2. แตกไฟล์ ZIP
3. **คัดลอก `js/main.js`** ใหม่ไปแทนที่ของเก่า
4. Refresh หน้าเว็บ

---

### ✅ วิธีที่ 4: ใช้ Incognito/Private Mode
1. เปิด Browser ในโหมด **Incognito** (Ctrl + Shift + N)
2. เปิด `http://localhost:8000`
3. ทดสอบใหม่

---

### ✅ วิธีที่ 5: เพิ่ม Cache Buster (แก้ถาวร)
เพิ่ม version parameter ใน `index.html`:

```html
<!-- เดิม -->
<script src="js/main.js"></script>

<!-- แก้เป็น -->
<script src="js/main.js?v=2"></script>
```

---

## การตรวจสอบว่าไฟล์อัปเดตหรือยัง

### ใน Browser Console (F12):
```javascript
// ตรวจสอบว่า API object มีหรือไม่
console.log(typeof API);  // ควรได้ "object"

// ตรวจสอบว่า generateMockLLMResponse ทำงานหรือไม่
console.log(typeof generateMockLLMResponse);  // ควรได้ "function"
```

---

## ถ้ายังไม่ได้ผล
1. ปิด Server (Ctrl + C ใน Terminal)
2. ลบไฟล์ `js/main.js` เก่า
3. Download ไฟล์ใหม่จาก Genspark
4. เปิด Server ใหม่: `start-server.bat`
5. เปิด Browser ในโหมด Incognito
6. ทดสอบอีกครั้ง

---

## Test Files สำหรับ Debug
- `test-prompt.html` - ทดสอบ Mock Response แยกจากระบบหลัก
- เปิดที่ `http://localhost:8000/test-prompt.html`
